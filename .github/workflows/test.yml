name: Test Contracts

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test Contracts
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: 1.89.0
        target: wasm32v1-none
        override: true
        # rustfmt + clippy are required by the Check format and Run
        # clippy steps below; actions-rs/toolchain@v1 only installs
        # rustc/cargo/rust-std by default.
        components: rustfmt, clippy
        profile: minimal

    - name: Install soroban-cli
      run: cargo install --locked soroban-cli --version 22.0.0

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo index
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache target directory
      uses: actions/cache@v4
      with:
        path: target
        key: ${{ runner.os }}-target-${{ hashFiles('**/Cargo.lock') }}

    - name: Run tests
      run: |
        cd contracts/ephemeral_account
        cargo test --verbose
        cd ../sweep_controller
        cargo test --verbose
        cd ../reserve_contract
        cargo test --verbose
        cd ../timelock_controller
        cargo test --verbose
        cd ../multisig_approval
        cargo test --verbose
        cd ../fee_splitter
        cargo test --verbose
        cd ../nonce_registry
        cargo test --verbose
        cd ../allowlist_registry
        cargo test --verbose
        cd ../audit_log
        cargo test --verbose
        cd ../version_registry
        cargo test --verbose
        cd ../asset_allowlist
        cargo test --verbose
        cd ../access_controller
        cargo test --verbose
        cd ../compliance_oracle
        cargo test --verbose
        cd ../claimable_balance_registry
        cargo test --verbose
        cd ../expiry_scheduler
        cargo test --verbose
        cd ../notification_registry
        cargo test --verbose

    - name: Check format
      run: |
        cd contracts/ephemeral_account
        cargo fmt -- --check
        cd ../sweep_controller
        cargo fmt -- --check
        cd ../reserve_contract
        cargo fmt -- --check
        cd ../timelock_controller
        cargo fmt -- --check
        cd ../multisig_approval
        cargo fmt -- --check
        cd ../fee_splitter
        cargo fmt -- --check
        cd ../nonce_registry
        cargo fmt -- --check
        cd ../allowlist_registry
        cargo fmt -- --check
        cd ../audit_log
        cargo fmt -- --check
        cd ../version_registry
        cargo fmt -- --check
        cd ../asset_allowlist
        cargo fmt -- --check
        cd ../access_controller
        cargo fmt -- --check
        cd ../compliance_oracle
        cargo fmt -- --check
        cd ../claimable_balance_registry
        cargo fmt -- --check
        cd ../expiry_scheduler
        cargo fmt -- --check
        cd ../notification_registry
        cargo fmt -- --check

    - name: Run clippy
      run: |
        cd contracts/ephemeral_account
        cargo clippy -- -D warnings
        cd ../sweep_controller
        cargo clippy -- -D warnings
        cd ../reserve_contract
        cargo clippy -- -D warnings
        cd ../timelock_controller
        cargo clippy -- -D warnings
        cd ../multisig_approval
        cargo clippy -- -D warnings
        cd ../fee_splitter
        cargo clippy -- -D warnings
        cd ../nonce_registry
        cargo clippy -- -D warnings
        cd ../allowlist_registry
        cargo clippy -- -D warnings
        cd ../audit_log
        cargo clippy -- -D warnings
        cd ../version_registry
        cargo clippy -- -D warnings
        cd ../asset_allowlist
        cargo clippy -- -D warnings
        cd ../access_controller
        cargo clippy -- -D warnings
        cd ../compliance_oracle
        cargo clippy -- -D warnings
        cd ../claimable_balance_registry
        cargo clippy -- -D warnings
        cd ../expiry_scheduler
        cargo clippy -- -D warnings
        cd ../notification_registry
        cargo clippy -- -D warnings

    - name: Build all contracts
      run: |
        cd contracts/ephemeral_account
        cargo build --target wasm32v1-none --release
        cd ../sweep_controller
        cargo build --target wasm32v1-none --release
        cd ../reserve_contract
        cargo build --target wasm32v1-none --release
        cd ../timelock_controller
        cargo build --target wasm32v1-none --release
        cd ../multisig_approval
        cargo build --target wasm32v1-none --release
        cd ../fee_splitter
        cargo build --target wasm32v1-none --release
        cd ../nonce_registry
        cargo build --target wasm32v1-none --release
        cd ../allowlist_registry
        cargo build --target wasm32v1-none --release
        cd ../audit_log
        cargo build --target wasm32v1-none --release
        cd ../version_registry
        cargo build --target wasm32v1-none --release
        cd ../asset_allowlist
        cargo build --target wasm32v1-none --release
        cd ../access_controller
        cargo build --target wasm32v1-none --release
        cd ../compliance_oracle
        cargo build --target wasm32v1-none --release
        cd ../claimable_balance_registry
        cargo build --target wasm32v1-none --release
        cd ../expiry_scheduler
        cargo build --target wasm32v1-none --release
        cd ../notification_registry
        cargo build --target wasm32v1-none --release

    - name: Upload WASM artifacts
      uses: actions/upload-artifact@v4
      with:
        name: wasm-contracts
        path: |
          contracts/ephemeral_account/target/wasm32v1-none/release/*.wasm
          contracts/expiry_scheduler/target/wasm32v1-none/release/*.wasm
          contracts/sweep_controller/target/wasm32v1-none/release/*.wasm
          contracts/reserve_contract/target/wasm32v1-none/release/*.wasm
          contracts/timelock_controller/target/wasm32v1-none/release/*.wasm
          contracts/multisig_approval/target/wasm32v1-none/release/*.wasm
          contracts/fee_splitter/target/wasm32v1-none/release/*.wasm
          contracts/nonce_registry/target/wasm32v1-none/release/*.wasm
          contracts/allowlist_registry/target/wasm32v1-none/release/*.wasm
          contracts/audit_log/target/wasm32v1-none/release/*.wasm
          contracts/version_registry/target/wasm32v1-none/release/*.wasm
          contracts/asset_allowlist/target/wasm32v1-none/release/*.wasm
          contracts/access_controller/target/wasm32v1-none/release/*.wasm
          contracts/compliance_oracle/target/wasm32v1-none/release/*.wasm
          contracts/claimable_balance_registry/target/wasm32v1-none/release/*.wasm
          contracts/notification_registry/target/wasm32v1-none/release/*.wasm
        retention-days: 30
